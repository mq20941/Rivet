#include<iostream>
#include<cmath>
#include <vector>


int main(){
    std::vector<vector<<double>> vec;
    vec.push_back(1);
    vec.push_back(2)
    for (size_t i = 0; i<vec.size(); ++i) {
        std::cout<<vec[i]<<std::endl;
    }
}

